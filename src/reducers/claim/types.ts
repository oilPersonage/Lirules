// import { GetClaimResponse } from '@api/claim';
// import { HttpErrorResponse } from '@api/types';

export type ClaimState = {
  // claim: Partial<GetClaimResponse>;
  // errorLoadingClaimData: HttpErrorResponse;
  claimDataLoading: boolean;
  claimDataLoaded: boolean;
};
